{
  "name": "gsti-iarx",
  "version": "1.0.0",
  "private": true,
  "description": "SaaS de Gestão para Gestores de TI - Financeiro, Projetos e SLA",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "test": "npm run test --workspace=server",
    "seed": "npm run seed --workspace=server",
    "start": "npm run start --workspace=server",
    "configurar": "node scripts/configurar.mjs",
    "iniciar": "npm run configurar && npm run build && npm start"
  },
  "engines": {
    "node": ">=22"
  }
}
